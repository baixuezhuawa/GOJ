# GOJ 编码约定

## 文档注释

本仓库新增或修改的代码必须添加详略得当、与实际行为同步的文档注释。

- 每个新增或修改的类都要添加 Javadoc，包括配置类、请求模型、语言适配器、
  Service 和异常类。
- 每个新增或修改的方法都要添加 Javadoc。公开方法和受保护方法需要说明用途、
  参数、返回值、可能抛出的异常以及可观察到的副作用。私有方法需要说明不明显的
  控制流、状态变化、资源所有权或协议行为。
- 每个新增或修改的属性都要在名称不够明确时添加 Javadoc，说明它的含义、单位、
  默认值、是否允许为空以及由哪一层负责维护。
- Judge 和沙箱代码必须明确资源单位。GOJ 业务模型使用 `ms` 和 `KB`；发送给
  go-judge 前如果转换为纳秒或字节，必须在注释中说明转换关系。
- 异步测评代码必须说明状态转换、重试行为、终态保护和清理责任。
- 注释应解释设计意图、约束和契约，不要逐行重复代码含义。
- 代码行为变化后，必须同步修改或删除过期的 TODO 和文档注释。

## 默认语言

- Javadoc、普通注释、日志文本、异常信息、用户可见提示和配置说明默认使用中文。
- 变量名、类名、方法名遵循 Java 的英文命名规范。
- 第三方协议字段名、语言编码、状态常量、URL 路径和外部 API 固定值保留其协议要求，
  不要为了中文化而修改。

## Judge 边界

- 用户源码必须在沙箱中执行。Server 和 Judge Worker 不得在 Spring Boot 进程或宿主机
  上直接执行用户提交的代码。
- 语言适配器负责构造语言相关的编译和运行请求；Judge Service 负责调用沙箱、解析
  测评结果、推进提交状态、比较输出以及清理沙箱缓存文件。
- Redis 队列消息使用 `submissionId` 作为契约；Judge Worker 必须从可信存储重新加载
  提交、题目、源码和测试数据。

## 分步讲解

- 给出具体实施步骤时，第一次出现的抽象概念（例如基类、接口、DTO、适配器、上下文、
  工厂或中间件）必须先用中文解释其含义，并说明它在当前项目中的真实类名、调用方、
  被调用方和负责解决的问题；不能只给出术语或代码。
- 在建议新增类、继承关系或方法之前，必须说明现有代码为什么不能直接完成、引入该结构
  后哪部分职责发生了移动，以及当前 MVP 是否真的需要它。能够用简单局部修改解决时，
  不要为了抽象而抽象。
- 每个可执行步骤应标明修改文件、前置条件、关键调用链和可观察的验证结果；代码示例中
  应区分项目自定义类与 Spring、MyBatis、go-judge 等框架或第三方类。
- 用户表示不理解某个术语时，后续说明必须回到项目源码，用一个实际的请求或状态流说明
  该术语，而不是继续使用未解释的抽象名称。

## 验证

- 修改 Java 代码后运行最小范围的 Maven 编译或测试命令。
- 修改 go-judge 请求构造时，增加或更新 JSON 断言，至少覆盖 `copyIn`、`copyOutCached`、
  资源限制和运行输入。

## 回复交付

- 当回复中识别出尚未实现、但会影响正确性、可靠性、可维护性或后续架构的工程债时，
  必须在回复末尾追加“工程债”列表。每项说明现状、后续目标和完成判据，供用户手动
  摘录到 `TODO.md`；除非用户明确要求，不自动修改用户笔记。
- 当回复涉及可操作的工程问题时，必须在回复末尾追加“开发日志候选”列表。每项至少
  包含问题、根因、解决方案和验证结果或待验证项，供用户手动摘录到 `开发日志.md`；
  除非用户明确要求，不自动修改用户笔记。
