<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gusl.gojserver.mapper.UserActivityDayMapper">


    <select id="getLastMonthAcceptedCount" resultType="java.lang.Long">
        SELECT COALESCE(SUM(new_solved_problem_count), 0)
        FROM user_activity_day
        WHERE user_id = #{userId}
          AND activity_date >= DATE_SUB(CURDATE(), INTERVAL 1 MONTH)
          AND activity_date &lt; DATE_ADD(CURDATE(), INTERVAL 1 DAY)
    </select>

    <select id="getLastYearAcceptedCount" resultType="java.lang.Long">
        SELECT COALESCE(SUM(new_solved_problem_count), 0)
        FROM user_activity_day
        WHERE user_id = #{userId}
          AND activity_date >= DATE_SUB(CURDATE(), INTERVAL 1 YEAR)
          AND activity_date &lt; DATE_ADD(CURDATE(), INTERVAL 1 DAY)
    </select>
</mapper>